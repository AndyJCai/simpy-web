import React from "react";
import styled from "styled-components";

interface SideProps {
  name: string;
  friends: number;
}

export default class Sidebar extends React.Component<SideProps> {
  render() {
    return <></>;
  }
}
